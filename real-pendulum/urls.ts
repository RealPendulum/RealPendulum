const baseURL = "http://localhost:3000/";
const homeURL = "/";
const infoURL = "info/";
const gameURL = "game/";
const examplesURL = "examples/";
const contactURL = "contact/";

const Urls = {
  baseURL,
  homeURL,
  infoURL,
  gameURL,
  examplesURL,
  contactURL,
};

export default Urls;
